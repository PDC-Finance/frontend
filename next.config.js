/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["placeimg.com", "www.isyssolutions.com", "dummyimage.com"],
  },
};

module.exports = nextConfig;
